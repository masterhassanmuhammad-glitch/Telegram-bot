services:
  - type: web

    name: botengine

    runtime: python

    plan: free

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      python main.py && uvicorn app:app --host 0.0.0.0 --port $PORT

    autoDeploy: true
